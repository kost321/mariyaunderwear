[build]
builder = "nixpacks"

[deploy]
startCommand = "node_modules/.bin/payload migrate && node_modules/.bin/next start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
